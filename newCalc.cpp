
#include "newCalc.h"
/*
newCalc::newCalc(string s, Command *com) : ComExp(com) {
    string numBuf;
    string varBuf;


    for (char c:s) {
        if (isdigit(c)) {
            numBuf += c;
            continue;
        }
        if (isLetter(c)) {
            varBuf += c;
            continue;
        }
        if (!numBuf.empty()) {
            this->q.push(Number(numBuf));
            numBuf = "";
        }
        if (!varBuf.empty()) {
            this->q.push(Number(this->c->getFromSymbolTable(varBuf)));
            varBuf = "";
        }
        if(isOperator(c)){

        }
        else if(isPar(c)){

        }



    }

}

bool newCalc::isLetter(char c) {
    return (c>='a' && c<='z')||(c>='A' && c<='Z');
}
*/